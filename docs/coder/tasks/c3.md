# Coder Task c3: Self-cost area damage skill (non-lethal to executor)

- **Source:** prompt (user, 2026-09-23): "implement skill effect that damage nearby enemies and on use effect damage executor health, but cannot kill executor, e.g. health cannot be zero"
- **Type:** feature
- **Status:** deferred
- **Started:** 2026-09-23
- **Finished:**
- **Session:** sess-2026-09-23-coder
- **Blocker (if deferred):** docs/architecture/architect-inbox/c3.md

## Goal

Add a skill effect that damages all nearby enemies (area) and also damages the executor's own health as a cost, guaranteeing the executor's health never reaches zero from this self-damage.

## Context

- Paradigm: `docs/architecture/paradigm.md`
- Orientation: `docs/architecture/orientation/index.md`
- Relevant subfiles: `modules.md` (Gameplay / Skill System, Characters), `conventions.md`
- Related hints: none yet
- Related coder tasks: `docs/coder/tasks/c1.md` (`AuraAreaDetector`/`DoubleStrikeAreaEffect`, same area pattern), `docs/coder/tasks/c2.md` (deferred; similar contract-shape concern re: `IDamageable`/`ExecutorData`)

## Inputs

- Files read: `ExecutorData.cs`, `Skill.cs`, `SkillConfig.cs`, `ISkillEffect.cs`, `AuraAreaDetector.cs`, `AuraSingleDetector.cs`, `NearestVampireDamageEffect.cs`, `DoubleStrikeAreaEffect.cs`, `IDamageable.cs`, `IReplenishable.cs`, `Health.cs`, `Player.cs`, `Enemy.cs`.

## Plan

Not started — task deferred before planning (blocker found during classification).

## Open questions

- Resolved by architect request (see blocker below); none outstanding for the coder side yet.

## Progress log

- 2026-09-23T00:15:00Z — card confirmed, classification and planning pending.
- 2026-09-24T00:00:00Z — resumed at Step 2 (Classify) per user request. Re-read task file, orientation index (cache matches HEAD `1a33243`), no relevant hints exist yet.
- 2026-09-24T00:05:00Z — read `IDamageable.cs`, `IReplenishable.cs`, `Health.cs`, `ExecutorData.cs`, `Player.cs`/`Enemy.cs` `TakeDamage`, `AuraAreaDetector.cs`, `DoubleStrikeAreaEffect.cs`, `Skill.cs`, `ISkillEffect.cs`, `SkillView.cs`. Confirmed: neither `IDamageable` nor `IReplenishable` exposes current health, so the "self-damage cannot reach zero" guarantee cannot be implemented through the contracts a skill effect is given without reaching past them to the concrete `Health` component.
- 2026-09-24T00:07:00Z — classification: blocker found. Trigger 3 (new/changed public contract) — same shape as c2's blocker. User chose "Defer to architect" via AskUserQuestion.
- 2026-09-24T00:08:00Z — wrote `docs/architecture/architect-inbox/c3.md`; set this task to deferred. Status: deferred.

## Outcome

- Files changed: `docs/architecture/architect-inbox/c3.md`, `docs/coder/tasks/c3.md` (this file). No code changed.
- Follow-ups: architect-role needs to decide the health-query contract shape (e.g. `IHealthQueryable` on `ExecutorData`, or extending `IDamageable`). Once resolved (`status: resolved` on c3.md), resume this coder task at Step 3 (Plan).
- Hints written: none.
