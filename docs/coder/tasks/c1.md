# Coder Task c1: Double-strike area damage skill effect

- **Source:** prompt (user, 2026-09-22): "implement the effect of a skill that deals 2 times one-time damage with a short interval to enemies in a radius"
- **Type:** feature
- **Status:** done
- **Started:** 2026-09-22
- **Finished:** 2026-09-22
- **Session:** sess-2026-09-22-coder

## Goal

Add a new `ISkillEffect` that hits every enemy in a radius with two discrete damage instances separated by a short interval.

## Context

- Paradigm: `docs/architecture/paradigm.md`
- Orientation: `docs/architecture/orientation/index.md`
- Relevant subfiles: `modules.md` (Gameplay / Skill System section), `conventions.md` (namespace/DI/coroutine conventions)
- Related ADRs: `docs/architecture/adr/0001-generalized-skill-system.md` (introduced `Skill`/`SkillConfig`/`ISkillEffect`/`ExecutorData`/`AuraSingleDetector`)
- Related hints: none yet (`docs/coder/hints/` did not exist before this task)

Existing `AuraSingleDetector` only returns the single nearest target, so it can't serve an AoE effect. `NearestVampireDamageEffect` is a per-frame DPS-style effect (`damagePerSecond * Time.deltaTime`), not a discrete one-time hit, so it's also not reusable as-is. Both are left untouched (not part of this task) since `AuraSingleDetector` is live-wired to the existing Vampire skill in the scene.

## Inputs

- Files read: `AuraSingleDetector.cs`, `NearestVampireDamageEffect.cs`, `ISkillEffect.cs`, `Skill.cs`, `SkillConfig.cs`, `ExecutorData.cs`, `IDamageable.cs`, an existing `.meta` file (for format).

## Plan

1. Add `Assets/Scripts/Gameplay/Skill System/AuraAreaDetector.cs` (+ `.meta`) — `MonoBehaviour`, `_layerToEffect` / `_radius` fields, `Radius` property, `FindTargetsInRadius(ExecutorData) : List<IDamageable>` via `Physics2D.OverlapCircleAll`, `OnDrawGizmosSelected` gizmo. Mirrors `AuraSingleDetector` structure.
2. Add `Assets/Scripts/Gameplay/Skill System/DoubleStrikeAreaEffect.cs` (+ `.meta`) — implements `ISkillEffect`, `[SerializeField] AuraAreaDetector _detector`, `[SerializeField, Min(0f)] float _damage`, `[SerializeField, Min(0f)] float _intervalInSeconds`. `Run`: damage all targets in radius, wait `_intervalInSeconds`, damage all targets in radius again.
3. Verify: `dotnet build` on `Assembly-CSharp.csproj`; `git status` limited to the 4 new files.

## Open questions

- None. Wiring a `SkillConfig` asset / prefab / scene GameObject for this effect is an Editor/content step outside code scope, not part of this task.

## Progress log

- 2026-09-22T00:00:00Z — plan approved, implementation not started.
- 2026-09-22T00:05:00Z — step 1 done: added `AuraAreaDetector.cs` + `.meta`; next: `DoubleStrikeAreaEffect.cs`.
- 2026-09-22T00:07:00Z — step 2 done: added `DoubleStrikeAreaEffect.cs` + `.meta`; next: build verification.
- 2026-09-22T00:10:00Z — `dotnet build Assembly-CSharp.csproj` succeeded, 0 errors/0 warnings. `git status` confirmed only the 4 new files (+ this task file) are new; no pre-existing tracked files touched. Status: done.

## Outcome

- Files changed: `Assets/Scripts/Gameplay/Skill System/AuraAreaDetector.cs`, `AuraAreaDetector.cs.meta`, `DoubleStrikeAreaEffect.cs`, `DoubleStrikeAreaEffect.cs.meta`.
- Follow-ups: wiring a `SkillConfig` asset, prefab, and scene GameObject (with `_damage`/`_intervalInSeconds`/`_radius`/`_layerToEffect` values) is an Editor/content step, not covered here.
- Hints written: none — no non-obvious gotcha surfaced beyond documented conventions.
